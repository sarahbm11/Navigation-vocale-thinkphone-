<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.thinkphone.navigation_vocale">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Obligatoire sur Android 14+ pour un service de premier plan de type micro -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Obligatoire sur Android 13+ pour afficher la notification du service -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Nécessaire pour ouvrir des applications -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Nécessaire pour la bulle flottante overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>
        <service
            android:name=".VoiceNavigationAccessibilityService"
            android:exported="true"
            android:label="Navigation Vocale Thinkphone"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".BackgroundSpeechService"
            android:exported="false"
            android:enabled="true"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".FloatingBubbleService"
            android:exported="false"
            android:enabled="true" />
    </application>
</manifest>
